"""Copywriting engines: titles and hooks.

Both turn an already-cut clip into something a viewer clicks and stays with,
and both are deliberately OUTSIDE the stage pipeline — they are cheap,
re-runnable on demand, and a user regenerating a title should never
invalidate a render.
"""
